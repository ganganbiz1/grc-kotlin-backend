spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/grc_test
    username: grc
    password: grc
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
